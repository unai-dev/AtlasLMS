@page "/login"

<AtlasAuthForm ButtonCaption="Iniciar Sesión" User="@user" OnSubmit="@HandleLogin" />

@code {
    private LoginDto? user = new();

    private void HandleLogin()
    {
        Console.WriteLine($"Iniciando sesión......");
    }
}
