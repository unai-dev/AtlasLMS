<div class="atlas-auth-page d-flex justify-content-center align-items-center">

    <div class="card shadow-lg p-5 auth-card bg-opacity-75 bg-white rounded-5">
        <EditForm EditContext="@editContext" OnValidSubmit="HandleValidSubmit">

            <DataAnnotationsValidator />

            <h3 class="text-center mb-4 text-danger">¡Bienvenido a AtlasLMS!</h3>

            <label class="form-label">Email</label>
            <InputText type="email" @bind-Value="@User.Email" placeholder="Introduce el email" class="form-control mb-3" />

            <label class="form-label">Contraseña</label>
            <PasswordInput @bind-Value="@User.Password" placeholder="Introduce la contraseña" />

            <div class="text-end mb-1">
                <a href="#" class="text-decoration-none text-danger small">¿Olvidaste tu contraseña?</a>
            </div>

            <button class="btn btn-danger w-100" disabled="@CurrentPost">
                @(CurrentPost ? "Iniciando Sesión..." : "Iniciar Sesión")
            </button>

            <footer class="text-center mt-3 text-muted small">
                © 2026 AtlasLMS - Software de Gestión de Librerías
            </footer>
        </EditForm>
    </div>
</div>
